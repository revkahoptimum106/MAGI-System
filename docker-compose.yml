services:
  magi:
    build: .
    container_name: magi
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
    networks:
      - magi-network

networks:
  magi-network: # Define the custom network
    name: magi-network
